namespace server.Model;

public record UserDTO(
                int UserId,
                string Name,
                string Email,
                string Institution,
                string Degree
                );

