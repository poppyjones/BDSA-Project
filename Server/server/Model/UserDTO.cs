namespace server.Model;

public record UserDTO(
                int Id,
                string Name,
                string Email,
                string Institution,
                string Degree
                );

